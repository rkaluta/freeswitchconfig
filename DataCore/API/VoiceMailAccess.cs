using System;
using System.Collections.Generic;
using System.Text;

namespace Org.Reddragonit.FreeSwitchConfig.DataCore.API
{
    //read_epoch = 0 for unread
    //read_epoch !=0 for saved -- epoch timestamp of time read
    //flags = "save" when saved
    //flags = 'delete' marks for deletion

    //voicemail paths = $base$/storage/voicemail/$Contxt$/$domain$/$extensionNumber$/

    public class VoiceMailAccess
    {
    }
}
